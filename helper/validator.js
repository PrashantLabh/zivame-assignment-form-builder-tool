const validator = (InputJson) => {

    // is Array


    // Individual Element had has id, type, text, 


    // filters invalid JSON

    // unique ids


    return true;
}

export default validator;

